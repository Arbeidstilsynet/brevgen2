# Brevgenerator2 Architecture

# Global connection label styling
(** -> **)[*].style.bold: true

# Actors
designer: {
  shape: person
  label: "Designer/fagperson"
  label.near: outside-top-center
  near: top-center
  width: 100
  height: 100
}

utvikler: {
  shape: person
  label: "Utvikler"
  label.near: outside-top-center
  width: 100
  height: 100
}

fagsystem: {
  label: "Fagsystem"
  label.near: top-center
  direction: down

  innholdsmaler: {
    label: "Innholdsmaler\nDynamic Markdown"
  }

  nuget: {
    label: "Arbeidstilsynet.Brevgenerator.Client\n(NuGet-pakke)"
    shape: package
    style.stroke-dash: 3
  }
}

# NAIS apps
preview-web: {
  label: "Brevgen2 editor\nbrevgenerator.arbeidstilsynet.no"
  shape: page
}

api: {
  label: "Brevgen2 API"
  label.near: top-center
  document-templates: "Dokumentmaler\nHTML+CSS"
  chromium: "Chromium (Puppeteer)"
}

# external services
external: {
  label: "Eksterne integrasjoner"
  label.near: bottom-right

  entra-id: {
    label: "Azure Entra ID"
    shape: cloud
  }

  repos: {
    label: "Repos"
    shape: cylinder
  }

  gcp-storage: {
    label: "GCP Storage"
    shape: cylinder
  }

  apertium: {
    label: "Apertium"
    shape: hexagon
  }
}

# Main flows
designer -> preview-web: "utformer innholdsmal"
designer -> utvikler: "sender innholdsmal"
utvikler -> fagsystem: "legger til innholdsmal i repo"
fagsystem.nuget -> api: "innholdsmal + variabler"
api -> fagsystem.nuget: "PDF" {
  style.stroke-dash: 3
}

preview-web -> api: "forhåndsvisning"
preview-web -> external.entra-id: "OIDC"
preview-web -> external.repos: "hente eksisterende innholdsmal"
preview-web -> external.gcp-storage: "lagre/hent WIP"
preview-web -> external.apertium: "oversett"

api -> external.entra-id: "JWKS"
